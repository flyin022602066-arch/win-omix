# oMLX Windows Requirements
# Install with: pip install -r requirements-windows.txt

# ONNX Runtime with DirectML for Windows GPU acceleration
onnxruntime-directml>=1.17.0

# Hugging Face Optimum for model optimization
optimum>=1.16.0
optimum-intel>=1.15.0

# Transformers and tokenizers
transformers>=4.40.0
tokenizers>=0.19.0
huggingface-hub>=0.23.0
accelerate>=0.29.0

# GGUF format support (llama.cpp backend)
llama-cpp-python>=0.2.70

# Core dependencies
numpy>=1.24.0
tqdm>=4.66.0
pyyaml>=6.0
itsdangerous>=2.0
jinja2>=3.0
sentencepiece
tiktoken
protobuf
requests>=2.28.0

# SOCKS proxy support
socksio>=1.0.0
tabulate>=0.9.0

# Resource monitoring (Windows compatible)
psutil>=5.9.0
pynvml>=11.5.0

# Server
fastapi>=0.100.0
uvicorn>=0.23.0

# JSON Schema validation for structured output
jsonschema>=4.0.0

# Harmony format parser for gpt-oss models
openai-harmony

# Image processing for VLM
Pillow>=9.0.0
opencv-python>=4.9.0

# Windows-specific dependencies
pywin32>=306
pystray>=0.19.5

# Quantization and optimization
bitsandbytes>=0.43.0

# Optional: MCP support
# mcp>=1.0.0

# Optional: ModelScope support
# modelscope>=1.10.0

# Optional: OpenVINO support
# optimum-openvino>=1.16.0
# openvino>=2024.0.0
